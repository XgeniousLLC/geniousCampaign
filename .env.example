# Copy this file to .env at the repo root and fill in values.
# Sprint 0/1 values are enough to run everything through Sprint 1 (npm run dev).
# Both apps/api (NestJS) and apps/web (Vite) read this single root .env.

# --- Sprint 0 ---

# Database / queue
DATABASE_URL=postgresql://localhost:5432/geniuscampaign_dev
REDIS_URL=redis://localhost:6379

NODE_ENV=development
PORT=3000

# apps/web reads this to reach apps/api locally
VITE_API_BASE_URL=http://localhost:3000

# --- Sprint 1 ---

# AWS SES
AWS_REGION=
SES_CONFIGURATION_SET=

# Cloudflare R2
CLOUDFLARE_R2_ACCOUNT_ID=
CLOUDFLARE_R2_ACCESS_KEY_ID=
CLOUDFLARE_R2_SECRET_ACCESS_KEY=
CLOUDFLARE_R2_BUCKET=
CLOUDFLARE_R2_PUBLIC_BASE_URL=

# Tracking
TRACKING_DOMAIN=track.yourdomain.com

# --- Sprint 3 ---

# Gmail Workspace sending
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=
GOOGLE_OAUTH_REDIRECT_URI=
ADMIN_APP_URL=
TOKEN_ENCRYPTION_KEY=
GMAIL_DEFAULT_DAILY_LIMIT=300

# Email verification
REOON_API_KEY=
NEVERBOUNCE_API_KEY=

# --- Sprint 4 ---
# GC-059 (AI-assisted template copy) is Blocked pending an LLM provider/API
# key decision from Sharifur — its env var(s) get added here once that's
# resolved (see docs/TICKETS.md GC-059).
